factorial:
	@cp ../retroImage .
	@../retro --with fact.rx --shrink >/dev/null

case:
	@cp ../retroImage .
	@../retro --with case.rx --shrink >/dev/null

fib:
	@cp ../retroImage .
	@../retro --with fib.rx --shrink >/dev/null

empty:
	@cp ../retroImage .
	@../retro --with empty.rx >/dev/null

smaller:
	@cp ../retroImage .
	@../retro --with empty.rx --shrink >/dev/null

loops:
	@cp ../retroImage .
	@../retro --with loop.rx --shrink >/dev/null
